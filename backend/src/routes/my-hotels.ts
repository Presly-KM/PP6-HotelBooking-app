import express, {Request, Response} from "express"; // Importation d'Express pour créer le serveur

const router = express.Router();

// api/my-hotels
router.post("/", async (req: Request, res: Response) => {
    
})